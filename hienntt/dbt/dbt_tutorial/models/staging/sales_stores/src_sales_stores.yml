version: 2

sources:
  - name: sales
    tables:
      - name: stores
        columns:
          - name: store_id
            description: Khóa bản ghi
            tests:
              - unique
              - not_null

models:
  - name: stg_sales_stores
    description: "Staging stores table"
    columns:
      - name: store_id
        description: "Store ID"
        tests:
          - unique
          - not_null